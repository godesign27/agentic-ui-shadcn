{
  "$schema": "../../../schemas/component-contract.schema.json",
  "schemaVersion": 1,
  "id": "ai:ai-response",
  "slug": "ai-response",
  "name": "AIResponse",
  "description": "patterns/ai-response.json as a component. Renders the required parts in the required order — header, optional work note, body or loading indicator, actions, feedback — so the pattern cannot be composed wrongly.",
  "intent": "One AI turn, assembled so attribution, progress and recourse are present by construction.",
  "status": "stable",
  "layer": "groups",
  "runtime": {
    "kind": "react",
    "platforms": [
      "web",
      "react"
    ],
    "importPath": "@/components/ai/ai-response",
    "exports": [
      "AIResponse"
    ],
    "implementation": "src/components/ai/ai-response.tsx"
  },
  "figma": {
    "fileKey": null,
    "nodeId": null,
    "url": null
  },
  "codeConnect": {
    "mapped": false,
    "url": null,
    "component": null
  },
  "props": [
    {
      "name": "agentLabel",
      "type": "string",
      "required": true,
      "hint": "Required. Declared in the component source."
    },
    {
      "name": "timestamp",
      "type": "string",
      "required": false,
      "hint": "Optional. Declared in the component source."
    },
    {
      "name": "loading",
      "type": "boolean",
      "required": false,
      "hint": "One AI turn, assembled so that authorship, progress and recourse are all present by construction. This is design-system/patterns/ai-response.json as a component: it renders the required parts in the required order, so the pattern cannot be composed wrongly. The header always precedes the body; the loading indicator announces; feedback is available unless explicitly suppressed. / export interface AIResponseProps extends React.HTMLAttributes<HTMLDivElement> { agentLabel: string timestamp?: string /** While true, the loading indicator replaces the body and announces politely."
    },
    {
      "name": "loadingVariant",
      "type": "\"thinking\" | \"working\" | \"retrieving\"",
      "required": false,
      "hint": "Optional. Declared in the component source."
    },
    {
      "name": "streaming",
      "type": "boolean",
      "required": false,
      "hint": "One AI turn, assembled so that authorship, progress and recourse are all present by construction. This is design-system/patterns/ai-response.json as a component: it renders the required parts in the required order, so the pattern cannot be composed wrongly. The header always precedes the body; the loading indicator announces; feedback is available unless explicitly suppressed. / export interface AIResponseProps extends React.HTMLAttributes<HTMLDivElement> { agentLabel: string timestamp?: string /** While true, the loading indicator replaces the body and announces politely. */ loading?: boolean loadingVariant?: \"thinking\" | \"working\" | \"retrieving\" /** While true, the body is a polite live region so arriving text is announced."
    },
    {
      "name": "workNote",
      "type": "object",
      "required": false,
      "hint": "One AI turn, assembled so that authorship, progress and recourse are all present by construction. This is design-system/patterns/ai-response.json as a component: it renders the required parts in the required order, so the pattern cannot be composed wrongly. The header always precedes the body; the loading indicator announces; feedback is available unless explicitly suppressed. / export interface AIResponseProps extends React.HTMLAttributes<HTMLDivElement> { agentLabel: string timestamp?: string /** While true, the loading indicator replaces the body and announces politely. */ loading?: boolean loadingVariant?: \"thinking\" | \"working\" | \"retrieving\" /** While true, the body is a polite live region so arriving text is announced. */ streaming?: boolean /** Optional disclosure of what the agent did to produce this."
    },
    {
      "name": "actions",
      "type": "FooterAction[]",
      "required": false,
      "hint": "Optional. Declared in the component source."
    },
    {
      "name": "showFeedback",
      "type": "boolean",
      "required": false,
      "hint": "One AI turn, assembled so that authorship, progress and recourse are all present by construction. This is design-system/patterns/ai-response.json as a component: it renders the required parts in the required order, so the pattern cannot be composed wrongly. The header always precedes the body; the loading indicator announces; feedback is available unless explicitly suppressed. / export interface AIResponseProps extends React.HTMLAttributes<HTMLDivElement> { agentLabel: string timestamp?: string /** While true, the loading indicator replaces the body and announces politely. */ loading?: boolean loadingVariant?: \"thinking\" | \"working\" | \"retrieving\" /** While true, the body is a polite live region so arriving text is announced. */ streaming?: boolean /** Optional disclosure of what the agent did to produce this. */ workNote?: { content?: string; items?: string[] } actions?: FooterAction[] /** Set false only where feedback genuinely has nowhere to go."
    },
    {
      "name": "onSentiment",
      "type": "(value: \"up\" | \"down\") => void",
      "required": false,
      "hint": "Optional. Declared in the component source."
    },
    {
      "name": "onCopy",
      "type": "() => void",
      "required": false,
      "hint": "Optional. Declared in the component source."
    },
    {
      "name": "bare",
      "type": "boolean",
      "required": false,
      "hint": "One AI turn, assembled so that authorship, progress and recourse are all present by construction. This is design-system/patterns/ai-response.json as a component: it renders the required parts in the required order, so the pattern cannot be composed wrongly. The header always precedes the body; the loading indicator announces; feedback is available unless explicitly suppressed. / export interface AIResponseProps extends React.HTMLAttributes<HTMLDivElement> { agentLabel: string timestamp?: string /** While true, the loading indicator replaces the body and announces politely. */ loading?: boolean loadingVariant?: \"thinking\" | \"working\" | \"retrieving\" /** While true, the body is a polite live region so arriving text is announced. */ streaming?: boolean /** Optional disclosure of what the agent did to produce this. */ workNote?: { content?: string; items?: string[] } actions?: FooterAction[] /** Set false only where feedback genuinely has nowhere to go. */ showFeedback?: boolean onSentiment?: (value: \"up\" | \"down\") => void onCopy?: () => void /** Renders without the AI surface wash — for a thread that is already marked."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "hint": "Merged with the component classes via cn(). Use for layout only — never to override a variant that already exists."
    }
  ],
  "variants": [],
  "states": [
    {
      "name": "Loading",
      "when": "`loading`"
    },
    {
      "name": "Streaming",
      "when": "`streaming`"
    },
    {
      "name": "Complete",
      "when": "`Neither`"
    },
    {
      "name": "Bare",
      "when": "`bare`"
    }
  ],
  "sizes": [],
  "tokens": {
    "semantic": [],
    "utilities": []
  },
  "a11y": {
    "role": "region",
    "keyboard": [
      "Tab through actions and feedback controls"
    ],
    "requiredAttributes": [
      "agentLabel"
    ],
    "notes": [
      "The region is labelled \"Response from {agentLabel}\", so a screen-reader user can identify and skip it.",
      "Actions and feedback are withheld while loading — offering them before there is output invites acting on nothing.",
      "The part order is fixed. Attribution before content is the property this component exists to guarantee."
    ],
    "liveRegion": "Loading announces politely; the body becomes a live region while streaming",
    "handledByRadix": false
  },
  "usage": [
    "Any AI response in a thread or panel",
    "Whenever you would otherwise hand-assemble header, body and feedback",
    "Streaming responses, via the streaming prop"
  ],
  "doNotUse": [
    "Human messages",
    "A consequential approval — use ai:ai-approval-card, which carries the confidence and rationale slots",
    "When you need a different part order; that order is the point",
    "Reordering the parts",
    "Consequential approvals"
  ],
  "governance": {
    "namespace": "ai",
    "brand": "Guild",
    "closedWorldIndexed": true,
    "agentRules": [
      "Prefer this over hand-assembling the parts — the order is the safety property.",
      "Set streaming only while text is arriving.",
      "Use bare only where the containing thread already marks AI authorship.",
      "Set showFeedback false only when feedback genuinely has nowhere to go."
    ],
    "related": [
      {
        "id": "ai:ai-approval-card",
        "note": "Consequential decisions"
      },
      {
        "id": "ai:ai-message-header",
        "note": "The parts, used directly"
      }
    ],
    "experienceMetadata": {
      "experienceMode": [
        "AI Assisted",
        "Adaptive",
        "AI Led"
      ],
      "aiBehavior": [
        "Suggest"
      ],
      "accountability": [
        "Attribution",
        "Rationale disclosure"
      ],
      "humanGestureRequired": false,
      "reversible": "not-applicable"
    }
  },
  "sources": {
    "spec": "design-system/components/ai/ai-response/ai-response.md",
    "manifest": "design-system/components/ai/ai-response/ai-response.agent.json",
    "implementation": "src/components/ai/ai-response.tsx",
    "prompt": "design-system/components/ai/ai-response/agentic-prompt.md",
    "preview": "design-system/components/ai/ai-response/ai-response.preview.html",
    "contract": "design-system/components/ai/ai-response/ai-response.contract.json"
  },
  "gaps": [
    {
      "field": "figma",
      "reason": "No Figma file, node, or Code Connect mapping in the spec or manifest."
    },
    {
      "field": "codeConnect",
      "reason": "No Code Connect mapping declared."
    },
    {
      "field": "variants",
      "reason": "No variants declared in manifest, source unions, or spec table."
    },
    {
      "field": "tokens",
      "reason": "No Guild semantic tokens declared in the manifest or extracted from TSX. Not guessed."
    }
  ],
  "origin": {
    "generated": true,
    "curated": false,
    "generatedAt": "2026-09-15T00:18:05.777Z",
    "from": [
      "md",
      "agent.json",
      "tsx"
    ]
  }
}
