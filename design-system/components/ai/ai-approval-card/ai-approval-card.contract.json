{
  "$schema": "../../../schemas/component-contract.schema.json",
  "schemaVersion": 1,
  "id": "ai:ai-approval-card",
  "slug": "ai-approval-card",
  "name": "AIApprovalCard",
  "description": "patterns/ai-approval-flow.json as a component. Attribution, then the proposal, then confidence, then rationale, then the actions — and only then.",
  "intent": "A consequential proposal, and the human decision about it, in an order that cannot be got wrong.",
  "status": "beta",
  "layer": "groups",
  "runtime": {
    "kind": "react",
    "platforms": [
      "web",
      "react"
    ],
    "importPath": "@/components/ai/ai-approval-card",
    "exports": [
      "AIApprovalCard"
    ],
    "implementation": "src/components/ai/ai-approval-card.tsx"
  },
  "figma": {
    "fileKey": null,
    "nodeId": null,
    "url": null
  },
  "codeConnect": {
    "mapped": false,
    "url": null,
    "component": null
  },
  "props": [
    {
      "name": "agentLabel",
      "type": "string",
      "required": true,
      "hint": "Required. Declared in the component source."
    },
    {
      "name": "timestamp",
      "type": "string",
      "required": false,
      "hint": "Optional. Declared in the component source."
    },
    {
      "name": "proposal",
      "type": "React.ReactNode",
      "required": true,
      "hint": "A consequential proposal, and the human's decision about it. This is design-system/patterns/ai-approval-flow.json as a component. The ordering is the safety property and is not configurable: attribution, then the proposal, then how sure the machine is, then how to see its reasoning, and only then the actions. A user who tabs straight to the primary button has still passed the confidence badge and the rationale link in reading order. Nothing fires without a human gesture. There is no autoApply prop, and adding one would violate AI_RULE_NO_SILENT_APPLY. / export interface AIApprovalCardProps extends Omit<React.HTMLAttributes<HTMLDivElement>, \"onError\"> { agentLabel: string timestamp?: string /** What the agent proposes. Stated plainly, in the agent's own words."
    },
    {
      "name": "confidence",
      "type": "ConfidenceLevel",
      "required": false,
      "hint": "A consequential proposal, and the human's decision about it. This is design-system/patterns/ai-approval-flow.json as a component. The ordering is the safety property and is not configurable: attribution, then the proposal, then how sure the machine is, then how to see its reasoning, and only then the actions. A user who tabs straight to the primary button has still passed the confidence badge and the rationale link in reading order. Nothing fires without a human gesture. There is no autoApply prop, and adding one would violate AI_RULE_NO_SILENT_APPLY. / export interface AIApprovalCardProps extends Omit<React.HTMLAttributes<HTMLDivElement>, \"onError\"> { agentLabel: string timestamp?: string /** What the agent proposes. Stated plainly, in the agent's own words. */ proposal: React.ReactNode /** Omit when the model produced no confidence value. Never guess one."
    },
    {
      "name": "risk",
      "type": "RiskLevel",
      "required": false,
      "hint": "Optional. Declared in the component source."
    },
    {
      "name": "staleData",
      "type": "boolean",
      "required": false,
      "hint": "Optional. Declared in the component source."
    },
    {
      "name": "missingSource",
      "type": "boolean",
      "required": false,
      "hint": "Optional. Declared in the component source."
    },
    {
      "name": "onWhyThis",
      "type": "() => void",
      "required": false,
      "hint": "A consequential proposal, and the human's decision about it. This is design-system/patterns/ai-approval-flow.json as a component. The ordering is the safety property and is not configurable: attribution, then the proposal, then how sure the machine is, then how to see its reasoning, and only then the actions. A user who tabs straight to the primary button has still passed the confidence badge and the rationale link in reading order. Nothing fires without a human gesture. There is no autoApply prop, and adding one would violate AI_RULE_NO_SILENT_APPLY. / export interface AIApprovalCardProps extends Omit<React.HTMLAttributes<HTMLDivElement>, \"onError\"> { agentLabel: string timestamp?: string /** What the agent proposes. Stated plainly, in the agent's own words. */ proposal: React.ReactNode /** Omit when the model produced no confidence value. Never guess one. */ confidence?: ConfidenceLevel risk?: RiskLevel staleData?: boolean missingSource?: boolean /** Wire this to real reasoning, or leave it off entirely."
    },
    {
      "name": "whyThisVariant",
      "type": "WhyThisVariant",
      "required": false,
      "hint": "Optional. Declared in the component source."
    },
    {
      "name": "primaryLabel",
      "type": "string",
      "required": true,
      "hint": "Required. Declared in the component source."
    },
    {
      "name": "secondaryLabel",
      "type": "string",
      "required": false,
      "hint": "Optional. Declared in the component source."
    },
    {
      "name": "tertiaryLabel",
      "type": "string",
      "required": false,
      "hint": "Optional. Declared in the component source."
    },
    {
      "name": "requiresReview",
      "type": "boolean",
      "required": false,
      "hint": "A consequential proposal, and the human's decision about it. This is design-system/patterns/ai-approval-flow.json as a component. The ordering is the safety property and is not configurable: attribution, then the proposal, then how sure the machine is, then how to see its reasoning, and only then the actions. A user who tabs straight to the primary button has still passed the confidence badge and the rationale link in reading order. Nothing fires without a human gesture. There is no autoApply prop, and adding one would violate AI_RULE_NO_SILENT_APPLY. / export interface AIApprovalCardProps extends Omit<React.HTMLAttributes<HTMLDivElement>, \"onError\"> { agentLabel: string timestamp?: string /** What the agent proposes. Stated plainly, in the agent's own words. */ proposal: React.ReactNode /** Omit when the model produced no confidence value. Never guess one. */ confidence?: ConfidenceLevel risk?: RiskLevel staleData?: boolean missingSource?: boolean /** Wire this to real reasoning, or leave it off entirely. */ onWhyThis?: () => void whyThisVariant?: WhyThisVariant primaryLabel: string secondaryLabel?: string tertiaryLabel?: string /** Shows the review signal above the actions. Use for anything consequential."
    },
    {
      "name": "status",
      "type": "\"default\" | \"loading\" | \"complete\" | \"disabled\" | \"error\"",
      "required": false,
      "hint": "Optional. Declared in the component source."
    },
    {
      "name": "errorMessage",
      "type": "string",
      "required": false,
      "hint": "A consequential proposal, and the human's decision about it. This is design-system/patterns/ai-approval-flow.json as a component. The ordering is the safety property and is not configurable: attribution, then the proposal, then how sure the machine is, then how to see its reasoning, and only then the actions. A user who tabs straight to the primary button has still passed the confidence badge and the rationale link in reading order. Nothing fires without a human gesture. There is no autoApply prop, and adding one would violate AI_RULE_NO_SILENT_APPLY. / export interface AIApprovalCardProps extends Omit<React.HTMLAttributes<HTMLDivElement>, \"onError\"> { agentLabel: string timestamp?: string /** What the agent proposes. Stated plainly, in the agent's own words. */ proposal: React.ReactNode /** Omit when the model produced no confidence value. Never guess one. */ confidence?: ConfidenceLevel risk?: RiskLevel staleData?: boolean missingSource?: boolean /** Wire this to real reasoning, or leave it off entirely. */ onWhyThis?: () => void whyThisVariant?: WhyThisVariant primaryLabel: string secondaryLabel?: string tertiaryLabel?: string /** Shows the review signal above the actions. Use for anything consequential. */ requiresReview?: boolean status?: \"default\" | \"loading\" | \"complete\" | \"disabled\" | \"error\" /** Shown beneath the actions when status is \"error\". Say what to do next."
    },
    {
      "name": "onPrimary",
      "type": "() => void",
      "required": false,
      "hint": "Optional. Declared in the component source."
    },
    {
      "name": "onSecondary",
      "type": "() => void",
      "required": false,
      "hint": "Optional. Declared in the component source."
    },
    {
      "name": "onTertiary",
      "type": "() => void",
      "required": false,
      "hint": "Optional. Declared in the component source."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "hint": "Merged with the component classes via cn(). Use for layout only — never to override a variant that already exists."
    }
  ],
  "variants": [],
  "states": [
    {
      "name": "Proposed",
      "when": "`status=\"default\"`"
    },
    {
      "name": "Requires review",
      "when": "`requiresReview`"
    },
    {
      "name": "Applying",
      "when": "`status=\"loading\"`"
    },
    {
      "name": "Applied",
      "when": "`status=\"complete\"`"
    },
    {
      "name": "Blocked",
      "when": "`status=\"disabled\"`"
    },
    {
      "name": "Failed",
      "when": "`status=\"error\"`"
    }
  ],
  "sizes": [],
  "tokens": {
    "semantic": [
      "destructive"
    ],
    "utilities": [
      "text-destructive"
    ]
  },
  "a11y": {
    "role": "region",
    "keyboard": [
      "Tab reaches rationale, then the actions"
    ],
    "requiredAttributes": [
      "agentLabel",
      "proposal",
      "primaryLabel"
    ],
    "notes": [
      "The order is the accessibility property. A user who tabs straight to the primary button has still passed the confidence badge and the rationale link in reading order.",
      "The region is labelled \"Approval requested by {agentLabel}\".",
      "There is no autoApply prop, and adding one would violate AI_RULE_NO_SILENT_APPLY. Nothing fires without a human gesture.",
      "Omitting confidence is correct when the model produced none. The component renders no badge rather than a default."
    ],
    "liveRegion": "errorMessage is role=\"alert\"",
    "handledByRadix": false
  },
  "usage": [
    "Any AI proposal a human must approve",
    "Agent-initiated changes to shared or outward-facing state",
    "Anywhere an agent needs authority it does not have"
  ],
  "doNotUse": [
    "Informational responses — use ai:ai-response",
    "Trivial reversible actions, where the ceremony is friction for nothing",
    "Destructive operations on their own; pair with ui:alert-dialog for the final confirmation",
    "Auto-applying on mount",
    "Invented confidence values",
    "A rationale link with nothing behind it",
    "An error state with no explanation",
    "Use for informational responses"
  ],
  "governance": {
    "namespace": "ai",
    "brand": "Guild",
    "closedWorldIndexed": true,
    "agentRules": [
      "Never auto-fire. There is no prop for it, and there must not be.",
      "Omit confidence when the model produced none — never pass a guess.",
      "Wire onWhyThis to real reasoning, or omit it.",
      "Set requiresReview for anything consequential.",
      "On failure, always supply errorMessage saying what to do next.",
      "Pair with ui:alert-dialog when the approved action is also destructive."
    ],
    "related": [
      {
        "id": "ai:ai-response",
        "note": "Informational turns"
      },
      {
        "id": "ai:ai-action",
        "note": "The decision row alone"
      },
      {
        "id": "ui:alert-dialog",
        "note": "Final confirmation for destructive actions"
      }
    ],
    "experienceMetadata": {
      "experienceMode": [
        "AI Assisted",
        "AI Led"
      ],
      "aiBehavior": [
        "Confirm",
        "Apply",
        "Approve"
      ],
      "accountability": [
        "Attribution",
        "Rationale disclosure",
        "Confidence signalling",
        "Approval",
        "Audit trail",
        "Reversibility"
      ],
      "humanGestureRequired": true,
      "reversible": "conditional"
    }
  },
  "sources": {
    "spec": "design-system/components/ai/ai-approval-card/ai-approval-card.md",
    "manifest": "design-system/components/ai/ai-approval-card/ai-approval-card.agent.json",
    "implementation": "src/components/ai/ai-approval-card.tsx",
    "prompt": "design-system/components/ai/ai-approval-card/agentic-prompt.md",
    "preview": "design-system/components/ai/ai-approval-card/ai-approval-card.preview.html",
    "contract": "design-system/components/ai/ai-approval-card/ai-approval-card.contract.json"
  },
  "gaps": [
    {
      "field": "figma",
      "reason": "No Figma file, node, or Code Connect mapping in the spec or manifest."
    },
    {
      "field": "codeConnect",
      "reason": "No Code Connect mapping declared."
    },
    {
      "field": "variants",
      "reason": "No variants declared in manifest, source unions, or spec table."
    }
  ],
  "origin": {
    "generated": true,
    "curated": false,
    "generatedAt": "2026-09-15T00:18:05.777Z",
    "from": [
      "md",
      "agent.json",
      "tsx"
    ]
  }
}
