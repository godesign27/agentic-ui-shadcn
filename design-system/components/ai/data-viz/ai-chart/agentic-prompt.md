# Agent prompt — AI Chart

Load `components/ai/data-viz/ai-chart/ai-chart.md` and `ai-chart.agent.json`.

This is a **stub** pending full Make → repo sync. Do not invent variants or props beyond the stub.
