{
  "$schema": "../../../schemas/component-contract.schema.json",
  "schemaVersion": 1,
  "id": "ui:resizable",
  "slug": "resizable",
  "name": "ResizableHandle",
  "description": "A react-resizable-panels wrapper. Panels, a group, and a handle that can show a grip.",
  "intent": "Let the user decide how to divide the space.",
  "status": "stable",
  "layer": "layout",
  "runtime": {
    "kind": "react",
    "platforms": [
      "web",
      "react"
    ],
    "importPath": "@/components/ui/resizable",
    "exports": [
      "ResizableHandle",
      "ResizablePanel",
      "ResizablePanelGroup"
    ],
    "implementation": "src/components/ui/resizable.tsx"
  },
  "figma": {
    "fileKey": null,
    "nodeId": null,
    "url": null
  },
  "codeConnect": {
    "mapped": false,
    "url": null,
    "component": null
  },
  "props": [
    {
      "name": "className",
      "type": "string",
      "required": false,
      "hint": "Merged with the component classes via cn(). Use for layout only — never to override a variant that already exists."
    }
  ],
  "variants": [],
  "states": [
    {
      "name": "Default",
      "when": "`Rest`"
    },
    {
      "name": "Dragging",
      "when": "`Pointer down on the handle`"
    },
    {
      "name": "Handle focused",
      "when": "`Keyboard focus`"
    }
  ],
  "sizes": [],
  "tokens": {
    "semantic": [
      "border",
      "ring"
    ],
    "utilities": [
      "bg-border",
      "ring-ring"
    ]
  },
  "a11y": {
    "role": "separator",
    "keyboard": [
      "Arrow keys — resize when the handle has focus",
      "Enter — collapse or expand where configured"
    ],
    "requiredAttributes": [
      "An accessible name on the handle when the panels are not otherwise identifiable"
    ],
    "notes": [
      "The library supplies keyboard resizing. The default handle is 1px wide — well under any reasonable target size.",
      "Use withHandle on touch surfaces so there is something to grab.",
      "Set minSize so a panel cannot be resized into uselessness."
    ],
    "handledByRadix": false
  },
  "usage": [
    "Split editor and preview layouts",
    "A resizable sidebar beside content",
    "Any layout where users have genuinely different space preferences"
  ],
  "doNotUse": [
    "Mobile — there is not enough room for the interaction to be worth it",
    "When a sensible fixed layout exists; resizing is a cost the user pays",
    "For content that reflows badly at arbitrary widths",
    "Resizable layouts on mobile",
    "Panels with no minSize"
  ],
  "governance": {
    "namespace": "ui",
    "brand": "Guild",
    "closedWorldIndexed": true,
    "agentRules": [
      "Set minSize on every panel.",
      "Use withHandle where touch or discoverability matters.",
      "Provide a non-resizable fallback for mobile.",
      "Persist the user's sizes if resizing is worth offering at all."
    ],
    "related": [
      {
        "id": "ui:sidebar",
        "note": "A purpose-built collapsible navigation panel"
      },
      {
        "id": "ui:separator",
        "note": "A static divider"
      }
    ]
  },
  "sources": {
    "spec": "design-system/components/ui/resizable/resizable.md",
    "manifest": "design-system/components/ui/resizable/resizable.agent.json",
    "implementation": "src/components/ui/resizable.tsx",
    "prompt": "design-system/components/ui/resizable/agentic-prompt.md",
    "preview": "design-system/components/ui/resizable/resizable.preview.html",
    "contract": "design-system/components/ui/resizable/resizable.contract.json"
  },
  "gaps": [
    {
      "field": "figma",
      "reason": "No Figma file, node, or Code Connect mapping in the spec or manifest."
    },
    {
      "field": "codeConnect",
      "reason": "No Code Connect mapping declared."
    },
    {
      "field": "variants",
      "reason": "No variants declared in manifest, source unions, or spec table."
    }
  ],
  "origin": {
    "generated": true,
    "curated": false,
    "generatedAt": "2026-09-15T00:18:05.777Z",
    "from": [
      "md",
      "agent.json",
      "tsx"
    ]
  }
}
