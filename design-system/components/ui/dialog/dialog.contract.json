{
  "$schema": "../../../schemas/component-contract.schema.json",
  "schemaVersion": 1,
  "id": "ui:dialog",
  "slug": "dialog",
  "name": "Dialog",
  "description": "Radix Dialog: a centred modal with an overlay, focus trap, Escape dismissal and focus return. Renders in a portal.",
  "intent": "Interrupt the user for a self-contained task that must finish before anything else continues.",
  "status": "stable",
  "layer": "organisms",
  "runtime": {
    "kind": "react",
    "platforms": [
      "web",
      "react"
    ],
    "importPath": "@/components/ui/dialog",
    "exports": [
      "Dialog",
      "DialogClose",
      "DialogContent",
      "DialogDescription",
      "DialogFooter",
      "DialogHeader",
      "DialogOverlay",
      "DialogPortal",
      "DialogTitle",
      "DialogTrigger"
    ],
    "implementation": "src/components/ui/dialog.tsx"
  },
  "figma": {
    "fileKey": null,
    "nodeId": null,
    "url": null
  },
  "codeConnect": {
    "mapped": false,
    "url": null,
    "component": null
  },
  "props": [
    {
      "name": "className",
      "type": "string",
      "required": false,
      "hint": "Merged with the component classes via cn(). Use for layout only — never to override a variant that already exists."
    }
  ],
  "variants": [],
  "states": [
    {
      "name": "Closed",
      "when": "`open={false}`"
    },
    {
      "name": "Open",
      "when": "`open={true}`"
    },
    {
      "name": "Closing",
      "when": "`Escape, overlay click, or close button`"
    }
  ],
  "sizes": [],
  "tokens": {
    "semantic": [
      "accent",
      "background",
      "muted",
      "ring"
    ],
    "utilities": [
      "bg-accent",
      "bg-background",
      "ring-ring",
      "text-muted"
    ]
  },
  "a11y": {
    "role": "dialog",
    "keyboard": [
      "Escape — close",
      "Tab — cycle within the trap",
      "Shift+Tab — reverse"
    ],
    "requiredAttributes": [
      "DialogTitle is mandatory — it supplies the accessible name"
    ],
    "notes": [
      "DialogTitle is not optional. If the design has no visible title, render one inside a VisuallyHidden wrapper.",
      "Do not nest a Dialog inside a Dialog. Replace the content instead.",
      "Page scroll is locked while open. Long content needs its own scroll container."
    ],
    "focusManagement": "Focus moves into the content on open and returns to the trigger on close. Radix handles both.",
    "handledByRadix": true
  },
  "usage": [
    "A short focused task — rename, invite, quick edit",
    "Content that genuinely requires the rest of the page to wait",
    "A form of one to four fields"
  ],
  "doNotUse": [
    "A multi-step workflow — that belongs on a page route. This is the most frequent misuse.",
    "A destructive confirmation — use ui:alert-dialog, which has the right semantics and focus default",
    "Contextual detail beside a trigger — use ui:popover or ui:hover-card",
    "An edge panel with room for a lot of content — use ui:sheet",
    "Anything the user might want to reach by URL",
    "DialogContent without DialogTitle",
    "Nested dialogs",
    "Multi-step wizards",
    "Destructive confirmation without alert-dialog semantics"
  ],
  "governance": {
    "namespace": "ui",
    "brand": "Guild",
    "closedWorldIndexed": true,
    "agentRules": [
      "DialogTitle is mandatory, visually hidden if necessary.",
      "Multi-step workflows go on a page route, not in here.",
      "Destructive confirmations use ui:alert-dialog.",
      "Never nest dialogs.",
      "Long content needs an inner scroll region, not a taller modal."
    ],
    "related": [
      {
        "id": "ui:alert-dialog",
        "note": "Destructive confirmation — Cancel is focused by default"
      },
      {
        "id": "ui:sheet",
        "note": "Edge panel with more room"
      },
      {
        "id": "ui:popover",
        "note": "Non-modal contextual content"
      },
      {
        "id": "ui:drawer",
        "note": "Not in this inventory — use ui:sheet with side=\"bottom\""
      }
    ]
  },
  "sources": {
    "spec": "design-system/components/ui/dialog/dialog.md",
    "manifest": "design-system/components/ui/dialog/dialog.agent.json",
    "implementation": "src/components/ui/dialog.tsx",
    "prompt": "design-system/components/ui/dialog/agentic-prompt.md",
    "preview": "design-system/components/ui/dialog/dialog.preview.html",
    "contract": "design-system/components/ui/dialog/dialog.contract.json"
  },
  "gaps": [
    {
      "field": "figma",
      "reason": "No Figma file, node, or Code Connect mapping in the spec or manifest."
    },
    {
      "field": "codeConnect",
      "reason": "No Code Connect mapping declared."
    },
    {
      "field": "variants",
      "reason": "No variants declared in manifest, source unions, or spec table."
    }
  ],
  "origin": {
    "generated": true,
    "curated": false,
    "generatedAt": "2026-09-15T00:18:05.777Z",
    "from": [
      "md",
      "agent.json",
      "tsx"
    ]
  }
}
