/**
 * DEPRECATED — removed.
 *
 * AIDialogButton (and the former AIButton `dialog` variant) are removed.
 * For low-emphasis toolbar triggers use: <AIButton variant="secondary" … />
 * See: ../button/AIButton.tsx
 *
 * This file is intentionally empty (kept only because the environment blocks
 * file deletion). Nothing imports it.
 */
export {};
