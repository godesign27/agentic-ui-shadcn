/**
 * DEPRECATED — removed (was a duplicate of AIChipQuick).
 *
 * Folded into the AIButton atom as `variant="secondary"` (quick-chip pill).
 * Use: <AIButton variant="secondary" size="sm" icon={<Icon />} label={…} />
 *
 * This file is intentionally empty (kept only because the environment blocks
 * file deletion). Nothing imports it.
 */
export {};
