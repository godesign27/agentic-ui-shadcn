/**
 * DEPRECATED — removed.
 *
 * AIChipQuick was folded into the AIButton atom as `variant="secondary"`
 * (the quick-chip pill). The special "All Prompts" chip is `special`.
 * Use: <AIButton variant="secondary" size="sm" icon={<Icon />} label={…} special? />
 * QUICK_ACTIONS now lives in ../button/AIButton.tsx.
 *
 * This file is intentionally empty (kept only because the environment blocks
 * file deletion). Nothing imports it.
 */
export {};
